{
  "$schema": "https://opencode.ai/config.json",
  "instructions": [
    "C:\\Users\\ignam\\.config\\opencode\\plugin\\shell-strategy\\shell_strategy.md"
  ],
  "command": {
    "learn": {
      "template": "Run /learn to extract reusable knowledge from this session.",
      "description": "Extract reusable knowledge from the session and create/update/fix reusable guidance",
      "agent": "general"
    },
    "commit": {
      "template": "Run the commit workflow for the current changes.",
      "description": "Commit workflow: delete implemented handoff, /update-project-rules, stage, format, commit, next handoff",
      "agent": "general"
    }
  },
  "mcp": {
    "playwright": {
      "type": "local",
      "command": ["npx", "-y", "@playwright/mcp@latest"],
      "enabled": true
    }
  }
}
